[package]
name = "oxbow"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
readme = "../README.md"

[dependencies]
arrow = "37.0.0"
noodles = { version = "0.35.0", features = ["bam", "bgzf", "core", "sam", "csi"] }
